<?xml version="1.0" encoding="utf-8"?>
<ScrollView android:layout_width="fill_parent" android:layout_height="fill_parent" android:overScrollMode="never"
  xmlns:android="http://schemas.android.com/apk/res/android">
    <LinearLayout android:orientation="vertical" android:layout_width="fill_parent" android:layout_height="wrap_content" android:divider="?android:listDivider" android:showDividers="middle|end">
        <LinearLayout style="@style/edit_password_record_model">
            <TextView android:text="@string/label_account" style="@style/password_record_text_label" />
            <TextView android:id="@id/account_description" style="@style/password_record_text" />
        </LinearLayout>
        <LinearLayout style="@style/edit_password_record_model">
            <TextView android:text="@string/label_user_name" style="@style/password_record_text_label" />
            <TextView android:id="@id/user_name" style="@style/password_record_text" />
        </LinearLayout>
        <LinearLayout style="@style/edit_password_record_model">
            <TextView android:text="@string/label_password" style="@style/password_record_text_label" />
            <TextView android:id="@id/password" style="@style/password_record_text" />
        </LinearLayout>
        <LinearLayout style="@style/edit_password_record_model">
            <TextView android:text="@string/label_notes" style="@style/password_record_text_label" />
            <TextView android:id="@id/notes" style="@style/password_record_text" />
        </LinearLayout>
    </LinearLayout>
</ScrollView>